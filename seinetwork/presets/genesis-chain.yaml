apiVersion: sei.io/v1alpha1
kind: SeiNetwork
spec:
  replicas: 4
  genesis: {}
  configOverrides:
    network.rpc.pprof_listen_address: "0.0.0.0:6060"
  resources:
    requests:
      cpu: "4"
      memory: 32Gi
  dataVolume:
    storage:
      resources:
        requests:
          storage: 500Gi
