apiVersion: sei.io/v1alpha1
kind: SeiNodeDeployment
spec:
  replicas: 2
  template:
    spec:
      fullNode: {}
      overrides:
        network.rpc.pprof_listen_address: "0.0.0.0:6060"
  updateStrategy:
    type: InPlace
